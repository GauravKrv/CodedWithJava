abstract public class Shape {
   /* ABSTRACTION:-
            1.Here we add abstract keyword before parent class name.
            2.Whenever any class becomes abstract we need to add an abstract function (which is empty),inside it.
            And the function mmust be implemented inside the child clas
            3. an abstract function cannot have a body inside the parent class as below declared.
  */  public Shape(){};
    public abstract void draw();

    ;

}
