public class V2ThreadExample {
    public static void main(String[] args) { //main thread

        Thread th = new Thread(); //creates a new thread
        th.start(); //will run a new thread in parallel to main thread
    }
}
